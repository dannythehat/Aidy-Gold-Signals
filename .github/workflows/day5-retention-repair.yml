name: AIDY Day 5 BigQuery Retention Repair

on:
  push:
    branches:
      - feature/day-5-historical-xauusd-backfill
    paths:
      - "ops/day5-retention-repair-trigger.txt"

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      AIDY_GCP_SERVICE_ACCOUNT_JSON: ${{ secrets.AIDY_GCP_SERVICE_ACCOUNT_JSON }}
      AIDY_BIGQUERY_DATASET: aidy_analytics_test
      AIDY_BIGQUERY_LOCATION: EU
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/day-5-historical-xauusd-backfill

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Run explicit retention repair and verification
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check --quiet 'google-cloud-bigquery>=3.42,<4'
          python scripts/day5_repair_bigquery_retention.py \
            > /tmp/day5-retention-repair.json \
            2> /tmp/day5-retention-repair.stderr

      - name: Persist repair evidence
        if: always()
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin feature/day-5-historical-xauusd-backfill
          git reset --hard origin/feature/day-5-historical-xauusd-backfill
          {
            echo '{"stdout":'
            cat /tmp/day5-retention-repair.json 2>/dev/null || echo 'null'
            echo ',"stderr":'
            python - <<'PY'
          import json
          from pathlib import Path
          path=Path('/tmp/day5-retention-repair.stderr')
          print(json.dumps(path.read_text(encoding='utf-8') if path.exists() else ''))
          PY
            echo '}'
          } > ops/day5-retention-repair-evidence.json
          git add ops/day5-retention-repair-evidence.json
          if ! git diff --cached --quiet; then
            git commit -m "Day 5: record BigQuery retention repair"
            git push origin HEAD:feature/day-5-historical-xauusd-backfill
          fi
