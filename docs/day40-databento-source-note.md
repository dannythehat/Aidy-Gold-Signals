# Databento source note

Day 40 uses Databento Historical HTTP API semantics verified on 2026-09-01: `GLBX.MDP3`; `metadata.get_cost` before download; parent symbology `GC.FUT`; OI-ranked continuous symbology `GC.n.0`; historical schemas include `ohlcv-1m`, `trades`, `tbbo`, `definition` and `statistics`. This note is informational only and is not part of the bounded Day 40 acceptance surface.
